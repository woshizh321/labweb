# Phase-1 backend skeleton. Pinned, minimal — no DB, no auth.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# Kawasaki_IVIG model inference (path B). scikit-learn is pinned EXACTLY to the
# version that serialized locked_final_model_pipeline.joblib (read from the joblib's
# embedded _sklearn_version); a mismatch triggers InconsistentVersionWarning or a
# load failure. numpy>=2 is required because the artifact pickles numpy._core.*.
scikit-learn==1.9.0
numpy>=2.0,<3
scipy>=1.13
joblib>=1.4
pandas>=2.2
openpyxl>=3.1       # parse uploaded .xlsx for the Kawasaki import-autofill
python-multipart>=0.0.9  # FastAPI file uploads (UploadFile)
